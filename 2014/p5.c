/*
	give your n sequantial pushes onto the statck,
	calculate how many arrangements of pops there are.

										by mBaturu
*/

#include <stdio.h>
#include <stdlib.h>




//m=items have not been pushed onto statck
//n=items already pushed onto stack

//uses bottom-up approach to analize the problem, top-down would not give your the answer.

//how many paths lead to a problem like this
int foo(int m,int n){
	if(m=1)return 1;




int main(){



















	return(0);
}
